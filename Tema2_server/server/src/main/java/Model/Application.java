package Model;
//import View.LoginPage;

public class Application {
	
	/*private static UserList userList = new UserList();
	private static InstitutiiList institutii = new InstitutiiList();
	
	
	/*public static void main(String[] args) {
		
		new CitireInstitutii().getInstitutii();
		for (Institutie i : getInstitutii().getInstitutii()) {
			System.out.println("Muzeu " + i.getId() + ", adresa: " + i.getAdresa() + ": ");
			for (OperaDeArtaPlastica o : i.getOpereDeArta()) {
				System.out.println("\t\t" + o.getTitlu() + " - " + o.getNumeArtist() + " - " + o.getAnulRealizarii());
			}
		}
		System.out.println();
		
		new CitireUser().getUsers();
		for (User user : getUserList().getUsers()) {
			System.out.println(user.getUsername() + ", password: " + user.getPassword());
		}
		//new LoginPage().getFrame().setVisible(true);
	}

	public static UserList getUserList() {
		return userList;
	}

	public static void setUserList(UserList userList1) {
		userList = userList1;
	}

	public static InstitutiiList getInstitutii() {
		return institutii;
	}

	public static void setInstitutii(InstitutiiList institutii) {
		Application.institutii = institutii;
	}*/
}
